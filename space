#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int main() {
    char a[100002];
    int count;
    scanf("%[^\n]s",a);
    for(int i=0;i<strlen(a);i++){
        if(a[i]==' ')
            count+=1;
    }
    printf("%d",count);
}
